<%= form_for(@table) do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% @table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tabname %><br>
    <%= f.text_field :tabname %>
  </div>

  <div class="field">
    <%= f.label :time %><br>
    <%= f.datetime_select :time, :default => Time.now + 7200, :minute_step => 15 %>
  </div>

  <div class="actions">
    <%= f.submit "OK" %>
  </div>
<% end %>
